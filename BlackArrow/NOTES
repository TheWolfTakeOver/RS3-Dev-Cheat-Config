Black Arrow is basically the standalone DLC to RS3, so it's not very surprising that the configs plugs in and works exactly the same.
To use a plug-and-play UMD file, just download this one and replace it in your system folder.
